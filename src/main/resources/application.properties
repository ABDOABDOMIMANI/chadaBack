# ================= Server Configuration =================
server.port=8080

# ================= Database Configuration =================
# Railway provides MYSQL_URL automatically when MySQL service is linked
# For local development, use the hardcoded connection string below
spring.datasource.url=${MYSQL_URL:jdbc:mysql://centerbeam.proxy.rlwy.net:24133/railway?useSSL=true&requireSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&autoReconnect=true}
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:sWYAcFfZevSnbVEqzBVugngHfPprsnLo}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Connection pool settings for better reliability
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.leak-detection-threshold=60000

# ================= JPA / Hibernate Configuration =================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=true

# ================= Logging =================
logging.level.root=INFO
logging.level.com.chada=DEBUG

# ================= File Upload Configuration =================
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=50MB
spring.servlet.multipart.file-size-threshold=2MB

# ================= File Storage Configuration =================
file.upload-dir=uploads/images
file.upload-dir.absolute=${user.dir}/${file.upload-dir}

# ================= Image Optimization Configuration =================
# Images will be automatically resized to max 1920x1920 and compressed
# JPEG quality: 85% (good balance between quality and file size)

# ================= Email Configuration =================
# Use environment variables for production (Railway), fallback to hardcoded values for local development
# IMPORTANT: In Railway, set these environment variables:
# - MAIL_HOST (default: smtp.gmail.com)
# - MAIL_PORT (default: 587)
# - MAIL_USERNAME (your Gmail address)
# - MAIL_PASSWORD (Gmail app password - NO SPACES!)
# - ADMIN_EMAIL (where to send notifications)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:khalidmimani@gmail.com}
# Remove spaces from password - Gmail app passwords work with or without spaces
spring.mail.password=${MAIL_PASSWORD:gwkdneycvkwomrqv}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=10000
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.writetimeout=10000
# Increase timeout for production
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
admin.email=${ADMIN_EMAIL:khalidmimani@gmail.com}

# ================= SMS Configuration =================
sms.phone.number=212656668002
sms.api.url=
sms.api.key=

# ================= CORS Configuration =================
